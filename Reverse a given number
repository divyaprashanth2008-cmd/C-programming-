#include <stdio.h>
int main() 
{
    int n, rev = 0, res;
    printf("Enter a number: ");
    scanf("%d", &n);
    while(n != 0) {
        res = n % 10;
        rev = rev * 10 + res;
        n /= 10;
    }
    printf("Reversed number = %d\n", rev);
    return 0;
}