#include <stdio.h>

int main() {
    int N;
    printf("Enter N: ");
    scanf("%d", &N);

    int sum = N * (N + 1) / 2;

    printf("Sum of series (1 + 2 + ... + %d) = %d\n", N, sum);

    return 0;
}
