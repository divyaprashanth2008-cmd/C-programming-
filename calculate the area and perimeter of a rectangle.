#include <stdio.h>
int main()
{
int l,b,perimeter;
printf("enter the length & breadth:");
scanf("%d%d",&l,&b);
float area=l*b;
perimeter=2*l+b;
printf("Area of the rectangle=%.2f\n",area);
printf("perimeter of the rectangle=%.2d",perimeter);
return 0;
}
