#include <stdio.h>

int main() {
    char str[100];
    int i=0, len=0, flag=1;

    scanf("%s", str);

    while(str[len]) len++;   // find length

    for(i=0; i<len/2; i++){
        if(str[i] != str[len-1-i]){
            flag=0;
            break;
        }
    }

    if(flag)
        printf("Palindrome");
    else
        printf("Not Palindrome");

    return 0;
}